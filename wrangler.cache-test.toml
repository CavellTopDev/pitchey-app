# Cache Testing Configuration
# Temporary configuration to test cache optimizations

name = "pitchey-cache-test"
main = "src/worker-cache-optimized.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
account_id = "e16d3bf549153de23459a6c6a06a431b"

[vars]
FRONTEND_URL = "https://pitchey-5o8.pages.dev"
ENVIRONMENT = "cache-testing"

# KV for cache storage - using existing production KV
[[kv_namespaces]]
binding = "KV"
id = "98c88a185eb448e4868fcc87e458b3ac"

# R2 for file storage (if needed)
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "pitchey-uploads"

# Durable Objects for WebSocket (if needed)
[[durable_objects.bindings]]
name = "WEBSOCKET_ROOMS"
class_name = "WebSocketRoom"

# Hyperdrive (if needed)
[[hyperdrive]]
binding = "HYPERDRIVE"
id = "983d4a1818264b5dbdca26bacf167dee"