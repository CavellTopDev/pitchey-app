// This file intentionally left as a module stub.
// react-global.ts (the .ts sibling) handles the actual React global setup.
// TypeScript only processes the .ts file when both exist with the same basename.
export {};