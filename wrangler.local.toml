name = "pitchey-local"
main = "src/worker-local.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Local development URLs
FRONTEND_URL = "http://localhost:5173"
ORIGIN_URL = "http://localhost:8001"
JWT_SECRET = "local-dev-secret-key"

# Optional local bindings (comment out if not needed)
# [[kv_namespaces]]
# binding = "CACHE"
# id = "local-kv-id"

# [[r2_buckets]]
# binding = "R2_BUCKET"
# bucket_name = "local-uploads"

# [[durable_objects.bindings]]
# name = "WEBSOCKET_ROOM"
# class_name = "WebSocketRoom"

# [[migrations]]
# tag = "v1"
# new_classes = ["WebSocketRoom"]